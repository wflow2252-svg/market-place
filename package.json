{
  "name": "marketplace-elite",
  "version": "1.0.0",
  "description": "Premium Marketplace Restoration Flow",
  "main": "api/index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "postinstall": "npx prisma generate --schema=backend/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.21.0",
    "express-rate-limit": "^8.3.1",
    "helmet": "^1.0.0",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^6.9.16",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
